# Phase 1 实施计划

1. 新增 `qingqing_v1/events.py` 进程内订阅总线。
2. `execution.py` 在步骤与 chat delta 时 publish。
3. `router.py` 增加 SSE 路由。
4. `qingqingApi.js` 增加 `streamAgentRun`；Chat 接入。
5. `AgentRunComposer` + Dashboard / Voice / Music / Video / Photo AI。
6. 后端 + 前端测试；提交。
