# 轻青设置页方案 3：设计 QA

## 验收上下文

- 视觉基准：`C:/Users/Lenovo/.codex/generated_images/019f56f6-b452-7641-aabc-7e8c64e0f7cb/exec-b14b9468-4453-42b0-98f9-78b362f259fa.png`
- 实现截图：`C:/Users/Lenovo/projects/qingqing/apps/qingqing_flutter/test/goldens/settings_page_desktop.png`
- 桌面视口：1440 x 1024
- 页面状态：已登录、高阶模式已开启、API 使用策略为“平台优先”
- 移动视口：390 x 844，由组件测试覆盖布局和策略保存交互

## 全屏对照

参考图与实现截图已在同一轮视觉输入中并排比较。当前实现对齐了以下视觉结构：

- 保留轻青桌面端侧边栏，不再跳转到脱离主框架的全屏设置页。
- 标题区、浅青安全主视觉、左侧调用偏好、右侧连接管理与使用说明形成稳定的双栏层级。
- 使用真实的安全盾牌与钥匙图片资源，没有用占位符或代码绘图替代。
- 三种 API 策略具有完整的选中、未选中状态；高阶模式开关和模型 API 入口保持可操作。
- 经过首轮对照后增加了卡片内边距和策略行高度，修正页面纵向过于紧凑的问题。

## 聚焦区域检查

- 主视觉：图片在桌面端保持右侧安全区，不遮挡标题与说明。
- 调用偏好：选中状态使用青色描边和浅青底色，单选层级清晰。
- 连接管理：入口、说明和“安全加密同步”提示收敛在单一卡片内。
- 使用说明：三条安全提示使用语义色区分，但不抢夺主操作注意力。
- 响应式：宽度小于 1040 时转为单栏；390 x 844 测试中无溢出并能保存 `byok_only`。

## 自动化证据

- `flutter test`：14 项通过，包括桌面结构、移动端交互和桌面视觉回归。
- `flutter analyze`：无问题。
- `flutter build web`：构建成功。
- `git diff --check`：通过，仅报告仓库既有的 LF/CRLF 提示。
- 本地 Web 与后端健康检查：`127.0.0.1:3001` 和 `127.0.0.1:8001/health` 均返回 200。

## 已知验收边界

当前会话没有可用的应用内浏览器执行接口。项目已明确选择应用内浏览器，因此没有擅自改用独立 Playwright 浏览器。Flutter golden 测试环境使用测试字体，中文显示为方块，只能用于几何布局和视觉回归，不能作为真实网页字体渲染证据。

因此尚未完成以下真实浏览器证据：

- 浏览器中中文字体、字重和换行的最终检查。
- 从首页进入设置页、切换策略、刷新后保持状态的端到端录屏或截图。
- 浏览器控制台与真实网络请求的检查。

final result: blocked
